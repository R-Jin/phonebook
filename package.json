{
  "name": "phonebook",
  "version": "0.0.1",
  "description": "Backend for phonebook application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../fullstack-open/part2/phonebook && npm run build && cp -r dist ../../../phonebook",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",    
    "logs:prod": "fly logs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0",
    "nodemon": "^3.1.1"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.7"
  }
}
